{
  "name": "pokevault-sync",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "node sync.mjs",
    "sync:dry": "node sync.mjs --dry-run",
    "sync:images": "node sync.mjs --phase 3",
    "sync:verbose": "node sync.mjs --verbose"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
